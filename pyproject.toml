[project]
name = "agent-system"
version = "0.1.0"
description = "Specialist Claude agent framework with structured handoffs"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0,<3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pyright>=1.1",
    "ruff>=0.4",
]
